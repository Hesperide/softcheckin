const assert = require('assert');

// Describe a set of tests
describe('Saving Tests', () => {
	
	//Set of tests below
	//Individual test
	it('Saving a record to the database', (done) => {
		assert(true);
		//TODO
		//
		//var char = new Char({ Construct Char here. });
		//char.save(); Mongoose Method. Asynchronous.
		//
		//Mongoose provides promise interface.
		//char.save().then( () => {
		//	assert(char.isNew === false);
		//	done(); //Mocha magic.
		//});
		done();
	});
});

